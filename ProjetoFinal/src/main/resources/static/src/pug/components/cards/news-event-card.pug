mixin news-card(img, type, text)
  .news-event-card
    figure
      |
      |
      img(src=img alt="thumb")/
      |
      |
    .desc
      .text
        p.type.caption #{$t.crypto_landing[type]}
        p #{text}
      |
      |
      a.btn.btn-small.btn-flat.waves-effect #{$t.crypto_landing.news_readmore}
      |
      |
